@model Explore

<div class="container">
    <div class="row">
        <div class="col-12">
            <form asp-action="DeleteExplore" method="post" class="card" style="width: 18rem;">
                <img src="~/uploads/explores/@Model.ImageUrl" class="card-img-top" alt="Image" />
                <div class="card-body">
                    <h5 class="card-title">@Model.Title</h5>
                    <h5 class="card-title">@Model.Subtitle</h5>
                    <p class="card-text">@Model.Description</p>
                    <input asp-for="@Model.Id" hidden value="@Model.Id" />
                    <a asp-action="index" asp-controller="explore" class="btn btn-warning">Go Back</a>
                    <button type="submit" class="btn btn-danger">Delete</button>
                </div>
            </form>
        </div>
    </div>
</div>